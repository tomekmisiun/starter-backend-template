# Copy to .env.observability for the local Grafana/Loki/Prometheus stack.
# docker-compose.observability.yml loads this file for the grafana service only.

GF_SECURITY_ADMIN_USER=admin
GF_SECURITY_ADMIN_PASSWORD=admin
GF_AUTH_ANONYMOUS_ENABLED=false
